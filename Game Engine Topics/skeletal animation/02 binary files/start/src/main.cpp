#include <iostream>
#include <fstream>
#include <sstream>

#include <json/json.h>
/*
	Skeletal Animation.
	Stage 2: Binary files.

	Goals:
		Open a binary file, read its contents to a vector of bytes.

		fetch 24 vec3s, at an offset of 288, byte length of 288.

		do the same thing, but fill an array of 72 float32s

		fetch 36 int16s, at an offset of 768, byte length of 72
*/

int main() {

	return 0;
}