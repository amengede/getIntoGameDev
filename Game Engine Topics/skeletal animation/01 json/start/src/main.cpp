#include <iostream>
/*
	Skeletal Animation.
	Stage 1: Json files.

	Goals:
			Open a json file, read it as a text file and print its contents.

			Open the same json file as a json file, and print contents in some more meaningful way.
*/

int main() {

	std::cout << "Hello world!" << std::endl;

	return 0;
}